apiVersion: v1
kind: Service
metadata:
  name: cadastro-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: cadastro-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
